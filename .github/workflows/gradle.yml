name: tests
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: compile
        run: javac ./src/PantryPal/*.java

      - name: run
        run: java APITest.java
      

