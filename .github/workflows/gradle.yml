name: tests
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: compile
        run: javac -cp "./src/PantryPal/" *.java

      - name: run
        run: java APITest.java
      

